name: PHPStan
on: [pull_request]
jobs:
  code_analysis:
    name: Debug
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
    - shell: bash
      env:
        JSON_DOC: ${{ toJSON(some.var) }}
      run: |
        printf '%s\n' "$JSON_DOC" > foo.json
        cat foo.json
